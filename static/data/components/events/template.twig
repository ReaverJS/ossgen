<section class="events">
    <div class="container">
        <h2>{{ title }}</h2>

        <div class="events-wrapper">
            {% for event in events %}
                <div class="event" data-link="{{ event.link }}">
                    <div class="text-wrapper">
                        <h5>{{ event.title }}</h5>
                        <p>🏦 {{ event.city }}</p>
                        <p>📌 {{ event.address }}</p>
                    </div>
                    <div class="date-wrapper">
                        <h5>{{ event.date }}</h5>
                        <p>{{ event.month }}</p>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>
</section>